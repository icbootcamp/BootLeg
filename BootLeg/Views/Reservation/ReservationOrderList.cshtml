@model List<BootLeg.Models.ReservationOrderModel>
@{
    ViewBag.Title = "ReservationOrderList";
    Layout = null;
}
<body>
    <h2>ReservationOrderList</h2>
    <div><button class="add-reserOrder-button btn btn-success">Add new Reservation Order</button></div><br />
    <table class="table table-bordered table-striped">
        <tr>
            <td>ID</td>
            <td>Reservation ID</td>
            <td>Order ID</td>
            <td>&nbsp;</td>
        </tr>
        @foreach (var x in Model)
        {
            <tr>
                <td id="dataid">@x.Id</td>
                <td id="dataresid">@x.ReservationId</td>
                <td id="dataorderid">@x.OrderId</td>
                <td>
                    <button class="edit-reserOrder-button btn btn-primary">Edit</button>
                    <button class="remove-reserOrder-button btn btn-warning">Remove</button>
                </td>
            </tr>
        }
    </table>
</body>


